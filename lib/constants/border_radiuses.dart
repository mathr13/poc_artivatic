import 'package:flutter/material.dart';

class BorderRadiuses {

  static const BorderRadiusGeometry borderRadius1 = BorderRadius.all(Radius.circular(30));
  static const BorderRadiusGeometry borderRadius2 = BorderRadius.all(Radius.circular(16));
  static const BorderRadiusGeometry borderRadius3 = BorderRadius.all(Radius.circular(12));

}